import {videos as defaultVideos, videoIds as defaultVideoIds} from '../res/data/videos';



export const videos = (state = defaultVideos,action) => {
  return state;
}


export const videoIds = (state = defaultVideoIds,action) => {
  return state;
}